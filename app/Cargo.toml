[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
proc-macro2 = "1.0"
markdown = { path = "../markdown" }
quote = "1.0"
syn = { version = "2", features = ["full", "extra-traits", "visit-mut"] }

[lib]
proc-macro = true
