# Generated by scripts/sync-claude-to-codex.py from .claude sources.
# Do not edit this file directly.

# Best-effort migration from .claude/settings.local.json permissions.allow.
# Only Bash(...) entries that can be represented as Codex prefix rules are included.
# Imported entries default to prompt because Codex rules govern out-of-sandbox execution.

# Skipped source entries that need manual review:
# - WebFetch(domain:crates.io)
# - WebFetch(domain:docs.rs)
# - WebFetch(domain:blog.takurinton.dev)
# - Read(//tmp/**)
# - WebFetch(domain:blog-takurinton-fbpdo8m56-takumi-katayamas-projects.vercel.app)
# - WebFetch(domain:qiita.com)
# - Bash(xargs -I {} bash -c 'echo "=== {} ===" && head -20 {}')

prefix_rule(
    pattern = ["gh", "api"],
    decision = "allow",
    justification = "Migrated from .claude permission: gh api",
)

prefix_rule(
    pattern = ["do"],
    decision = "prompt",
    justification = "Migrated from .claude permission: do",
)

prefix_rule(
    pattern = ["mkdir"],
    decision = "prompt",
    justification = "Migrated from .claude permission: mkdir",
)

prefix_rule(
    pattern = ["chmod", "+x"],
    decision = "prompt",
    justification = "Migrated from .claude permission: chmod +x",
)

prefix_rule(
    pattern = ["gh", "pr"],
    decision = "allow",
    justification = "Migrated from .claude permission: gh pr",
)

prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Migrated from .claude permission: git push",
)

prefix_rule(
    pattern = ["cargo", "build"],
    decision = "prompt",
    justification = "Migrated from .claude permission: cargo build",
)

prefix_rule(
    pattern = ["cargo", "check"],
    decision = "prompt",
    justification = "Migrated from .claude permission: cargo check",
)

prefix_rule(
    pattern = ["git"],
    decision = "allow",
    justification = "Migrated from .claude permission: git",
)

prefix_rule(
    pattern = ["cargo", "test"],
    decision = "prompt",
    justification = "Migrated from .claude permission: cargo test",
)

prefix_rule(
    pattern = ["gh", "run"],
    decision = "allow",
    justification = "Migrated from .claude permission: gh run",
)

prefix_rule(
    pattern = ["cargo", "fmt"],
    decision = "prompt",
    justification = "Migrated from .claude permission: cargo fmt",
)

prefix_rule(
    pattern = ["ls"],
    decision = "allow",
    justification = "Migrated from .claude permission: ls",
)

prefix_rule(
    pattern = ["cargo", "run"],
    decision = "prompt",
    justification = "Migrated from .claude permission: cargo run",
)

prefix_rule(
    pattern = ["cp"],
    decision = "prompt",
    justification = "Migrated from .claude permission: cp",
)

prefix_rule(
    pattern = ["npm", "init"],
    decision = "prompt",
    justification = "Migrated from .claude permission: npm init",
)

prefix_rule(
    pattern = ["npm", "install"],
    decision = "prompt",
    justification = "Migrated from .claude permission: npm install",
)

prefix_rule(
    pattern = ["/tmp/trace.txt"],
    decision = "prompt",
    justification = "Migrated from .claude permission: /tmp/trace.txt",
)

prefix_rule(
    pattern = ["find"],
    decision = "allow",
    justification = "Migrated from .claude permission: find",
)

prefix_rule(
    pattern = ["grep"],
    decision = "allow",
    justification = "Migrated from .claude permission: grep",
)

prefix_rule(
    pattern = ["gh", "issue"],
    decision = "allow",
    justification = "Migrated from .claude permission: gh issue",
)

prefix_rule(
    pattern = ["gh", "workflow"],
    decision = "allow",
    justification = "Migrated from .claude permission: gh workflow",
)
