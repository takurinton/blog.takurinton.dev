#!/bin/bash
# User prompt submit hook - runs when user submits a message
# Can be used to inject context or run checks

# Example: Check if dist/ is outdated
# if [ -d "./dist" ]; then
#     echo "Note: dist/ exists. Run 'cargo run' to regenerate if needed."
# fi

exit 0
